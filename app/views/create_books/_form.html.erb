<%= form_with(model: create_book) do |form| %>
  <% if create_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(create_book.errors.count, "error") %> prohibited this create_book from being saved:</h2>

      <ul>
        <% create_book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
